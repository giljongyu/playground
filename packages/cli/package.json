{
  "name": "@playground/cli",
  "version": "0.0.1",
  "description": "Clean monorepo CLI tool",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "clean-mono": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rimraf dist",
    "cli": "node dist/index.js"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsup": "^8.0.2",
    "rimraf": "^5.0.5",
    "@repo/typescript-config": "workspace:*"
  }
}
